
import * as store from "./store.validator"
import * as category from "./category.validator"
import * as product from "./product.validator"
import * as cart from "./cart.validator"
import * as user from "./user.validator"

export const validator = {
    store,
    category,
    product,
    cart,
    user
}